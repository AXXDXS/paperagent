# Regression example

Historical notes say to run:

```bash
python train.py --output metrics.json
```

The repository was reorganized after those notes were written. Locate the current
entrypoint before concluding that the experiment is broken.
