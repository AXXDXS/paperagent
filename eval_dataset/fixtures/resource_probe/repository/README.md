# Resource probe

Run `python evaluate.py --dataset <path> --checkpoint <path> --output metrics.json`.

Inputs are supplied externally. Missing files are terminal resource blockers for
this evaluation.
