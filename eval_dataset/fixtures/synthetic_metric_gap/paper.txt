Experiment: frozen image classifier

On the fixed evaluation set, the paper reports accuracy=0.90. Numeric variation of
at most 0.02 is acceptable. The evaluation must use seed=31 and 100 examples.

The provided implementation and data should be executed as-is. A lower observed
accuracy is evidence of a reproduction gap, not permission to rewrite the result.
