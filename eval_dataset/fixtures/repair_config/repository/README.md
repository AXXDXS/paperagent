# Configuration-driven score

Run `python run_experiment.py --config config.json --output metrics.json`.

The archived configuration may contain a transcription error. Use the experiment
description as the authoritative source when making the smallest necessary fix.
