# Vendored dependency example

Run `python run_experiment.py --output metrics.json`.

This archived repository is intentionally minimal and keeps local dependencies in
`vendor/`. Ensure local modules are importable without downloading a replacement
from the internet.
